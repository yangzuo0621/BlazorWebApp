@page "/grid"

<Grid Data="@data">
    <Columns>
        <Column Field="ID"></Column>
        <Column Field="TheName" Title="Employee Name"></Column>
    </Columns>
</Grid>

@code {
    public IEnumerable<object> data = Enumerable.Range(1, 50).Select(x => new { ID = x, TheName = "name " + x });
}